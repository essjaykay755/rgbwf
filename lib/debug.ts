// Debug utility functions for authentication

/**
 * Debug function to log authentication state
 * This is a placeholder function that can be expanded for more detailed debugging
 */
export const debugAuthState = async () => {
  if (process.env.NODE_ENV === 'development') {
    console.log('Debug: Checking auth state')
  }
  return true
} 